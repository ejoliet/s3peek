interface:
  display_name: "S3peek"
  short_description: "Repo-specific patterns and workflows for s3peek"
  default_prompt: "Use the s3peek repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true